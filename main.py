import subprocess

# Run fetch.py
subprocess.run(['python', 'fetch.py'])

# Run clean.py
subprocess.run(['python', 'clean.py'])

# Run merge1.py
subprocess.run(['python', 'merge1.py'])

# Run merge2.py
subprocess.run(['python', 'merge2.py'])
